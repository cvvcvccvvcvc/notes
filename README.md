# Notes

Личное offline-first приложение для расписания, дел и свободных заметок. Данные
сразу сохраняются в IndexedDB текущего браузера и затем синхронизируются с личным
сервером. Потеря сети не блокирует работу.

Production: <https://notes.135-106-167-202.sslip.io>

## Установка

Требуется Node.js 22.13 или новее.

```sh
npm install
```

Для разработки доступны `npm run dev` и `npm run dev:client`; серверу нужны
переменные из `deploy/.env.example`.

Опциональный seeded preview запускается командой `npm run preview`. Он собирает
и запускает полное приложение и печатает локальный адрес по шаблону
`http://YYYY-MM-DD.preview.localhost:8788` с текущей датой. Пароль — `preview`.
При первом
запуске preview создаёт в `.local-preview` локальную базу с расписанием, делами,
заметками, шаблонами и историей. В течение дня следующие запуски сохраняют
изменения из предыдущего тестирования, а с новой датой обновляют относительные
дни демо-набора и меняют локальный origin, чтобы браузер не подмешивал вчерашнее
offline-состояние. Эта база не попадает в Git и не связана с production.
Preview остаётся вспомогательным инструментом и не требуется для обычной работы
или передачи изменений.

Для проверки production-сборки:

```sh
npm run build
npm start
```

Перед передачей изменений:

```sh
npm run verify
```

Изменённые пользовательские сценарии дополнительно проходят вручную. Не храните
в приложении единственную копию важных данных до появления проверенного
восстановления из серверной резервной копии.

## Документация

- [Правила продукта](docs/product.md) — подтверждённое поведение и открытые
  продуктовые вопросы.
- [Архитектура](docs/architecture.md) — границы, модель данных, синхронизация и
  аутентификация.
- [Эксплуатация](docs/operations.md) — безопасное развёртывание на Selectel,
  резервное копирование и откат.
- [Версии и ветки](docs/versioning.md) — `main`, production-релизы и SemVer.
- [AGENTS.md](AGENTS.md) — короткий маршрут для работающих с кодом агентов.
